# Modules

module=> set of files(abstraction)
tf not recursive, so doesnt go into dir
working with modules => working with something outside the working dir

modules can be sourced from various places: local, git, link, bucket etc

each module is versioned, part of the block syntax